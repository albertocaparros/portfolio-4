---
const { tag, textSize = 'base', isButton = false } = Astro.props;
---

<li
  class={`text-${textSize} list-none bg-sky-50  dark:bg-slate-800 border-slate-600 dark:border-blue-200 border rounded-full py-1 px-4 ${isButton && 'cursor-pointer hover:bg-sky-200 dark:hover:bg-slate-600'}`}
>
  {tag}
</li>
